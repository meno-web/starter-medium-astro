---
import { cx, resolveProps } from 'meno-astro';
import { Link } from 'meno-astro/components';

const __props = resolveProps(Astro, {
  text: { type: "string", default: "Button" },
  link: { type: "link", default: { href: "#" } }
});
const { text, link, class: className } = __props;

const __meno = { acceptsStyles: true };
---
<Link href={link} class={cx("p-(--b-p) inline-flex flex-row justify-start items-stretch bg-accent text-[var(--white)] rounded-(--b-br) text-(length:--te-fs)", className)}>{text}</Link>
